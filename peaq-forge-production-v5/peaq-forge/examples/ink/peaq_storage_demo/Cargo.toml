[package]
name    = "peaq_storage_demo"
version = "1.0.0"
authors = ["peaq Forge"]
edition = "2021"

[dependencies]
ink = { version = "5", default-features = false }

[dev-dependencies]
ink_e2e = { version = "5" }

[lib]
path = "src/lib.rs"

[features]
default     = ["std"]
std         = ["ink/std"]
ink-as-dependency = []
e2e-tests   = []
