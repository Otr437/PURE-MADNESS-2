---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference-pay
  namespace: arc-agents
  labels:
    app: inference-pay
    app.kubernetes.io/part-of: arc-agent-monorepo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inference-pay
  template:
    metadata:
      labels:
        app: inference-pay
    spec:
      containers:
        - name: inference-pay
          image: arc-agents/inference-pay:latest
          ports:
            - containerPort: 3004
          envFrom:
            - secretRef:
                name: arc-agent-secrets
          env:
            - name: PORT
              value: "3004"
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: inference-pay-service
  namespace: arc-agents
spec:
  selector:
    app: inference-pay
  ports:
    - port: 3004
      targetPort: 3004
