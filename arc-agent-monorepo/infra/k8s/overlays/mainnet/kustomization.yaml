apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

namespace: arc-agents-mainnet

configMapGenerator:
  - name: arc-chain-config
    literals:
      - ARC_RPC_URL=https://rpc.arc.network/
      - ARC_CHAIN_ID=5042001
      - CIRCLE_ENV=production
